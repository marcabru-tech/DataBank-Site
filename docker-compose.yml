version: "3.9"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./data/db.sqlite
      - LOG_LEVEL=info
      - PERSIST_LEADS=false
    volumes:
      - ./data:/app/data
